# Dependencias del proyecto
python-escpos>=3.1
Pillow>=10.0.0
pywin32>=300  # Para instancia única y funciones de Windows

# Nota: python-escpos requiere:
# - Pillow (instalado automáticamente)
# - PyYAML (instalado automáticamente)
# - Otras dependencias (instaladas automáticamente)

# Para códigos QR en tickets (fallback como imagen si la impresora no soporta QR nativo):
qrcode>=7.4
